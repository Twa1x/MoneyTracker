<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoneyTracking.Views.HomePage">
    <ContentPage.Content>


        <StackLayout  VerticalOptions="FillAndExpand" HorizontalOptions="Fill">

            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="Center">
                <Image Source="piggy_bank.png" BackgroundColor="White" VerticalOptions="Center" HeightRequest="150" />
            </StackLayout>
            
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
              
                    <Label Text="Welcome,"></Label>
            </StackLayout>
           

            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <Label Text="{Binding UserName, Mode=TwoWay}"></Label>
            </StackLayout>


            <StackLayout >
                <Entry Placeholder="Spent" Text="{Binding Spent, Mode=TwoWay}" FontSize="16"></Entry>
            </StackLayout>
            <StackLayout>
                <Entry Placeholder="Price" Text="{Binding Price, Mode=TwoWay }" FontSize="16" ></Entry>
            </StackLayout>
            <StackLayout >
                <Entry Placeholder="Data" Text="{Binding Data, Mode=TwoWay }" FontSize="16" ></Entry>
            </StackLayout>

 

            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="EndAndExpand">
                <Button Text="LogOut" Clicked="Button_Clicked" Command="{Binding LogOutCommand}"></Button>
            
            </StackLayout>
            
            


        </StackLayout>
    </ContentPage.Content>
</ContentPage>