<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoneyTracking.Views.HomePage"
             xmlns:dg="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid"  >
    

    <ContentPage.Content>
       
        <StackLayout  VerticalOptions="FillAndExpand" HorizontalOptions="Fill">

            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="Center">
                <Image Source="piggy_bank.png" BackgroundColor="White" VerticalOptions="Center" HeightRequest="150" />
            </StackLayout>
            
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
              
                    <Label Text="Welcome,"></Label>
            </StackLayout>
           
        
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <Label Text="{Binding UserName, Mode=TwoWay}"></Label>
            </StackLayout>
            <StackLayout>
                <dg:DataGrid>
                  
                      <dg:DataGrid.Columns>
                        <dg:DataGridColumn Title="Name" PropertyName="Name" Width="1*"></dg:DataGridColumn>
                        <dg:DataGridColumn Title="Amount of money" PropertyName="Amount of money" Width="2*"/>
                        <dg:DataGridColumn Title="Data" PropertyName="Data" Width="2*"/>
                        <dg:DataGridColumn Title="Type" PropertyName="Type" Width="2*"/>
                    </dg:DataGrid.Columns>
                    
                    <dg:DataGrid.RowsBackgroundColorPalette>
                        <dg:PaletteCollection>
                            <Color>#F2F2F2</Color>
                            <Color>#FFFFFF</Color>
                        </dg:PaletteCollection>
                    </dg:DataGrid.RowsBackgroundColorPalette>
                </dg:DataGrid>
            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="EndAndExpand">
                <Button Text="LogOut"  Command="{Binding LogOutCommand}"></Button>
            
            </StackLayout>
            
            


        </StackLayout>
    </ContentPage.Content>
</ContentPage>